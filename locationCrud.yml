apiVersion: v1
kind: ReplicationController
metadata:
  labels:
    name: location-crud
  name: bcpa-location-controller
spec:
  replicas: 1
  template:
    metadata:
      labels:
        name: location-crud
    spec:
      containers:
        - image: downeys/bcpa-location-crud
          name: location-crud
          ports:
            - name: location-crud
              containerPort: 8080
---
#Node Port Service
apiVersion: v1
kind: Service
metadata:
  labels:
    name: location-crud
  name: location-crud
spec:
  type: NodePort
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    name: location-crud